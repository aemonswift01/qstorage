# gstore 顶点 RowID 分配、哈希冲突处理、与 array 的结合

## 一、RowID 的位结构

RowID 是一个 64 位整数（`src/include/storage/gdm_rowid.h:7-26`），按从低位到高位分为三段：

```
| 16 bits: 表ID (table_id) | 12 bits: hashcode | 36 bits: 数组ID (array_id) |
  bit[48..63]                bit[36..47]          bit[0..35]
```

- `ARRAY_ID_SIZE  = 36`，偏移 0  —— 顶点/边在段内数组里的下标
- `HASH_CODE_SIZE = 12`，偏移 36 —— 用来做**分段（segment）路由**，范围 [0, 4095]
- `TABLE_ID_SIZE  = 16`，偏移 48 —— label / 表的字典 ID

构造函数（`gdm_rowid.h:46-56`）：
```c
INLINE t_uint64 new_rowid(t_uint16 table_id, t_uint16 hash_code, t_uint64 array_id) {
    row_id  = (array_id  & ARRAY_ID_MASK)  << 0;
    row_id |= (hash_code & HASH_CODE_MASK) << 36;
    row_id |= (table_id  & TABLE_ID_MASK)  << 48;
    return row_id;
}
```

注意：顶点的 `array_id` 实际只用低 32 位（`MAX_VERTEX_ARRAY_ID = INVALID_ARRAY_ID_INT32`，`gdm_rowid.h:36`），而边用满 36 位。

---

## 二、创建一个顶点时 RowID 是怎么分配的

### 2.1 调用链（从上到下）

| 阶段 | 函数 | 文件:行 |
|---|---|---|
| 顶层批量插入 | `op_put_run` | `src/op/gdm_op_put.c:1037` |
| 单条 tuple 处理 | `_put_tuple` | `src/op/gdm_op_put.c:810` |
| **RowID 分配** | `get_id_for_tuple` | `src/storage/gdm_vlog.c:306-367` |
| 写入 vlog | `vlog_append_tuple` | `src/storage/gdm_vlog.c:409` |
| 提交期更新 hash 索引 | `hash_index_process_tuple` | `src/segment/gdm_seg_manager.c:718` |
| 写入顶点数组 | `garray_insert_vertex_for_tuple` ← `graph_put_vertex` | `src/storage/gdm_garray.c:1560` |

### 2.2 RowID 三段各自的来源（`gdm_vlog.c:313-320`，TABLE_TYPE_LABEL 分支）

```c
tuple->key->row_id = new_rowid(
    tuple->iid,                                                // (a) table_id
    murmur_hash3_32_to_12(tuple->key->hash_code),              // (b) hash_code (12 bit)
    ver_get_next_vertex_id(segment->ver_sys)                   // (c) array_id
);
```

**(a) `table_id = tuple->iid`**
来自字典：label/表的内部 ID，由 parser/executor 在生成 tuple 时填好，与 `t_dict_index->persistent.id` 一致。

**(b) `hash_code (12 bit) = murmur_hash3_32_to_12(tuple->key->hash_code)`**
- `tuple->key->hash_code` 是 **32 位**主键哈希，由 `tuple_key_hashcode`（`src/dta/gdm_tuple.c:805-822`）按"逐列 MurmurHash3_32 → `dta_hashcode_combine` (即 `murmur_hash3_mix32`) 混合"得到。
- `murmur_hash3_32_to_12`（`src/utils/gdm_murmurhash.c:182`）把 32 位折叠成 12 位。
- **哈希的输入是主键列的值**，与 array_id 完全无关。

**(c) `array_id = ver_get_next_vertex_id(segment->ver_sys)`**
```c
INLINE t_uint32 ver_get_next_vertex_id(t_ver_sys* ver_sys) {
    return os_interlock_inc_fetch(&ver_sys->meta.last_vertex_id);   // 原子自增
}
```
（`src/include/fsm/gdm_version.h:548`）

**关键点：array_id 是段内单调递增的原子计数器，与哈希冲突、桶号都无关。** 每来一条 INSERT 就 `+1`，永不回退，分配不可能失败（除非 32 位溢出）。

`prev_id` 字段则取 `garray_get_last_id(..., LS_REC_TYPE_LABEL, iid)` —— 同 label 链表的"上一个 vid"，用于 label 级单向链表。

---

## 三、RowID 的 hash_code 与哈希索引的桶号是同一回事吗？

**不是。** 它们从同一个 32 位主键哈希派生，但用途和变换都不同：

| 字段 | 变换 | 范围 | 用途 |
|---|---|---|---|
| RowID 中的 12-bit `hash_code` | `murmur_hash3_32_to_12(hash_code)` | [0, 4095] | **定位 segment**（`is_hash_cur_segment`，`src/segment/gdm_seg_manager.c:488`） |
| 哈希索引的 `slot` | `(fold ^ mask1 << 32 \| fold ^ mask2) % slot_count` | [0, slot_count) | **定位段内桶**（`_hash_index_table_calc_hash`，`src/storage/gdm_hash_index.c:986`） |

`_hash_index_process_insert_low`（`gdm_hash_index.c:2085-2086`）用的就是 32 位原始哈希：
```c
fold = tuple->key->hash_code;            // 32 位
slot = _hash_index_table_calc_hash(hash_index->table, fold);
```

所以一条记录落盘时：
1. 先按 **12-bit hash** 选 segment；
2. segment 内再按 **32-bit hash mod slot_count** 选桶；
3. 桶内冲突用拉链解决（下一节）。

---

## 四、遇见哈希冲突怎么处理？

### 4.1 冲突检测：拉链遍历 `_hash_index_key_search`（`gdm_hash_index.c:1780-1890`）

```c
while (key_off != SINT40_UNDEFINED) {
    hrec = hash_index_get_hrec(hash_index, key_off, ...);
    // (1) 逐记录校验：delimiter == 0xFF + checksum（崩溃恢复）
    // (2) 主键字节比较：hrec_th_cmp(key, hrec, desc, key->n_keys)
    if (ret == NCMP_IS_EQU) {
        if (!cmp_id || hrec_get_arrayid(hrec) == rowid_get_array_id_int36(key->row_id)) {
            return key_off;          // 真正命中
        }
    }
    key_off = hrec_get_prev_offset(hrec);   // 沿 prev_offset 走链
}
```

**严格命中条件**：主键字节相等 **且** array_id 相等（`cmp_id=TRUE` 时）。所以即便两条记录主键相同（理论上 PK 不会，但非唯一索引可能），只要 array_id 不同就是两条不同的记录。

### 4.2 冲突消解：链表 + 头/尾插

新记录追加到 Key 文件末尾（`_hash_index_buf_append`，`gdm_hash_index.c:2439`），然后挂到桶的链上：

| 模式 | 插法 | 实现 | 风险 |
|---|---|---|---|
| 内存态（mmap_flush=FALSE，导入/重建） | **头插法** | `_hash_index_key_write_head`，`gdm_hash_index.c:1696/2547` | 断链风险——但崩溃时本来就要全量重建 |
| mmap 态（mmap_flush=TRUE，稳态运行） | **尾插法** | `_hash_index_key_write_tail`，`gdm_hash_index.c:2543` | 无断链风险，崩溃只丢最后半条 |

设计权衡见 `gdm_hash_index.c:1981-2039` 的中文注释：
- 头插法只需知道桶内偏移，**O(1)**，但断链不可恢复 → 仅用于可重建场景
- 尾插法必须先查到链尾，但**增量安全**

### 4.3 顶点创建时 RowID 分配会因为冲突失败/重试吗？

**不会。** `get_id_for_tuple`（`gdm_vlog.c:306`）无条件原子自增 `last_vertex_id`，没有任何冲突检查、没有重试。哈希冲突只影响哈希索引的链长，不影响 RowID 分配。

### 4.4 那冲突会让顶点创建被拒绝吗？

**只有主键唯一性冲突会被拒**，并且**在分配 RowID 之前**就检查完了。流程在 `_put_tuple`（`gdm_op_put.c:810-954`）：

1. 如果索引是主键（label 总是 PK）：
   - `trx_check_and_put_key` 查事务内冲突
   - `op_unique_check` → `hash_index_op_unique_check`（`gdm_hash_index.c:2914`）→ `_hash_index_key_search_ex(slot, cmp_id=FALSE)`
2. 命中已有相同主键 → 按冲突策略：
   - `UNIQUE_CONFLICT_ERROR`   → 返回 `GDM_EC_DUP_KEY`
   - `UNIQUE_CONFLICT_RESERVE` → 返回 `GDM_EC_PUT_CONFLICT_WARN`
   - `UNIQUE_CONFLICT_OVERWRITE` → **复用已有 tuple 的 row_id**，把 xop 改成 `TRX_OP_UPDATE`，**不再分配新 RowID**
3. 没命中 → 走 `get_id_for_tuple` 分配新 RowID

注意 OVERWRITE 分支：`tuple->key->row_id` 在到达 `get_id_for_tuple` 之前就已被赋值，而 `get_id_for_tuple` 与 `vlog_append_tuple` 里都有 `row_id == INVALID_ROW_ID` 的短路检查（`gdm_op_put.c:942`，`gdm_vlog.c:431`），所以 **UPDATE 路径根本不会调用 `ver_get_next_vertex_id`**。

### 4.5 顶点数组的二次保护

`garray_insert_vertex`（`src/storage/gdm_garray.c:1480-1557`）会检查 `vid` 这个槽是否已有有效记录：
```c
if (vrec_is_valid(vt_rec2.rec) && vrec_get_offset(vt_rec1.rec) != INVALID_OFFSET) {
    return GDM_EC_VERTEX_EXIST;
}
```
这是**数组槽位占用检查**，不是哈希冲突——只有在同一 `vid` 重复写入时触发（正常自增分配不会撞）。`graph_put_vertex`（`src/storage/gdm_graph.c:41-54`）把它也接上 conflict_policy。

---

## 五、RowID 与 array 是怎么结合的

### 5.1 顶点数组（vertex array）

每个 segment 维护一个**变长记录数组**，下标就是 RowID 的 `array_id`（对顶点即 32 位 `vid`）。`garray_insert_vertex(vid, ...)` 把顶点记录写到 `vid` 对应的物理位置（`gdm_garray.c:1480`、`_garray_insert_vertex_low:1397`、`_garray_insert_vertex_fast:1372`）。

写入的 `vt_rec`（顶点记录）包含：
- `l_id`（label id）
- `offset`（指向 vlog 中真实数据的偏移）
- `prev_vid`（同 label 链表前驱，来自 `tuple->key->prev_id`）
- `hash_code`（与 RowID 同源的 12 位 hash）
- `flag / xid`（事务可见性）

### 5.2 哈希索引里的 arrayid 就是 RowID 的 array_id

`hrec_encode`（`src/fsm/gdm_hrec.c:82-83`）把 RowID 拆开存进 hrec：
```c
hrec_set_arrayid(hrec, rowid_get_array_id_int36(tuple->key->row_id));
hrec_set_hashcode(hrec, rowid_get_hash_code(tuple->key->row_id));
```

`hrec_decode`（`gdm_hrec.c:138`）反向重建：
```c
key->row_id = new_rowid(key->iid, hrec_get_hashcode(hrec), hrec_get_arrayid(hrec));
```

所以 hrec 头部的 `arrayid`（5 字节）和 `hashcode`（2 字节）**就是 RowID 的对应字段**，存的是同一个值。

### 5.3 两次查询路径

**按主键查顶点（PK → RowID）**：
```
hash_index_op_unique_check
  ├─ slot = _hash_index_table_calc_hash(table, key.hash_code)
  ├─ _hash_index_key_search_ex(slot)            // 走拉链，比对主键字节
  └─ hrec_decode(hrec) → key.row_id             // 拿到 RowID（含 array_id）
```

**按 RowID 取顶点数据（RowID → 顶点记录）**：
```
garray_get_vertex_info_inner(vid = rowid_get_array_id(row_id), ...)
  └─ 直接以 vid 为下标读顶点数组    // O(1)，无需走哈希
```

哈希索引是"**主键 → RowID**"的映射；顶点数组是"**RowID → 数据**"的下标访问。两者通过 RowID 中的 `array_id` 字段衔接。

### 5.4 segment 定位也用 RowID

不必走哈希，直接从 RowID 取 12 位 hash 就能算出 segment（`get_segment_id_by_rowid`，`src/segment/gdm_seg_manager.c:136-159, 480-486`）。这样基于 RowID 的查询（如边找端点）省掉了重算主键哈希的开销——这也是为什么要把 hash 冗余进 RowID。

---

## 六、`last_vid` 的两套计数器

容易混淆，这里澄清——存在两处 `last_vid`：

| 位置 | 类型 | 作用 |
|---|---|---|
| `ver_sys->meta.last_vertex_id` | **原子自增**（`os_interlock_inc_fetch`） | **唯一分配源**，每次 INSERT `+1` |
| `hash_index->cur_meta->last_vid` | **原子取 max**（`meta->last_vid = MAX(meta->last_vid, array_id)`） | 仅用于 checkpoint/刷盘记账 |

后者在 `_hash_index_update_meta_by_tuple`（`gdm_hash_index.c:1710-1740`）按每次插入更新，启动时由前者初始化（`hash_index_sys_pre_init`，`gdm_hash_index.c:38-40`）。它**不分配 ID**，只是个高水位线，写进文件头偏移 16 处（`HASH_INDEX_META_LAST_VID`）用于崩溃恢复后重建。

---

## 七、端到端流程图

```
op_put_run
  └─ _put_tuple(segment, tuple)
       │
       ├─ [PK 唯一性预检]
       │   trx_check_and_put_key(trx, tuple)
       │   op_unique_check(...)
       │     └─ hash_index_op_unique_check
       │           └─ _hash_index_key_search_ex(slot)        // 走拉链
       │                 └─ 已有相同主键 →
       │                       ERROR / RESERVE / OVERWRITE(复用旧 row_id)
       │
       ├─ [分配 RowID] (仅当 row_id == INVALID_ROW_ID)
       │   get_id_for_tuple
       │     ├─ table_id  = tuple->iid
       │     ├─ hash_code = murmur_hash3_32_to_12(tuple->key->hash_code)  // 12 bit
       │     └─ array_id  = ver_get_next_vertex_id(ver_sys)               // 原子 ++
       │     ⇒ new_rowid(table_id, hash_code, array_id)
       │
       ├─ trx_append_tuple → vlog_append_tuple (写 WAL)
       │
       └─ [提交期]
           index_process_tuple → hash_index_process_tuple
                 └─ _hash_index_process_insert_low
                       ├─ slot = (fold ^ mask1 << 32 | fold ^ mask2) % slot_count   // 32 bit
                       ├─ htfile_row_lock(slot % 32)
                       └─ hash_index_key_insert
                             ├─ _hash_index_key_search(slot)  // 重复检查
                             └─ hash_index_tuple_append
                                   ├─ hrec_encode
                                   │     ├─ arrayid  = rowid_get_array_id(row_id)   // 同一值
                                   │     └─ hashcode = rowid_get_hash_code(row_id)
                                   └─ _hash_index_buf_append
                                         ├─ 追加到 Key 文件
                                         ├─ write_head/tail(slot, off)             // 拉链挂接
                                         └─ meta->last_vid = MAX(meta->last_vid, array_id)

           [顶点数组写入（可与上面异步）]
           graph_put_vertex
             └─ garray_insert_vertex_for_tuple
                   └─ garray_insert_vertex(vid = rowid_get_array_id(row_id))
                         ├─ 槽位已占 → VERTEX_EXIST
                         └─ _garray_insert_vertex_low: 写 vt_rec1/2/3 到数组[vid]
```

---

## 八、速查表

| 主题 | 文件 | 行号 |
|---|---|---|
| RowID 位结构定义 | `src/include/storage/gdm_rowid.h` | 7-26 |
| `new_rowid` 构造 | `src/include/storage/gdm_rowid.h` | 46-56 |
| RowID 分配主入口 | `src/storage/gdm_vlog.c` | 306-367 |
| 顶点 array_id 自增 | `src/include/fsm/gdm_version.h` | 548-551 |
| 32 位主键哈希计算 | `src/dta/gdm_tuple.c` | 805-822 |
| 32→12 位哈希折叠 | `src/utils/gdm_murmurhash.c` | 182 |
| 段路由（按 12 位 hash） | `src/segment/gdm_seg_manager.c` | 488-501 |
| 桶号计算（按 32 位 hash） | `src/storage/gdm_hash_index.c` | 986-997 |
| 冲突链遍历 | `src/storage/gdm_hash_index.c` | 1780-1890 |
| 头插/尾插 | `src/storage/gdm_hash_index.c` | 1667-1707, 1981-2039 |
| hrec 编码（存 RowID 字段） | `src/fsm/gdm_hrec.c` | 82-83 |
| hrec 解码（重建 RowID） | `src/fsm/gdm_hrec.c` | 138 |
| 顶点数组写入 | `src/storage/gdm_garray.c` | 1372, 1397, 1480, 1560 |
| PK 唯一性预检 | `src/op/gdm_op_put.c` | 810-954 |
| 唯一性检查实现 | `src/storage/gdm_hash_index.c` | 2914-2959 |
| hash index meta 的 last_vid | `src/storage/gdm_hash_index.c` | 1710-1740 |

---

## 九、一句话回答三个问题

1. **RowID 怎么分配**：`table_id` 来自字典、`hash_code` 是主键的 MurmurHash3 折叠到 12 位、`array_id` 是段内原子自增计数器 `ver_get_next_vertex_id`，三者拼成 64 位 RowID。

2. **哈希冲突怎么处理**：RowID 分配本身不受冲突影响（自增计数器永不冲突）；哈希索引内部的冲突用**拉链法**——内存态头插、mmap 态尾插，靠 hrec 的 `prev_offset` 串链，链上比对"主键字节 + array_id"双重判定。

3. **RowID 与 array 怎么结合**：RowID 的 `array_id` 字段同时是**顶点数组的下标**和**哈希索引 hrec 的 arrayid 字段**——哈希索引负责"主键 → RowID"的映射，顶点数组负责"array_id → 数据"的 O(1) 访问，两者通过同一组字段衔接。
