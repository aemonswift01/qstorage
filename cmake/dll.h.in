#pragma once

// clang-format off
#ifdef @LIB_NAME@_STATIC
    #define @LIB_NAME@_API
#else
    #if defined(_WIN32) || defined(_WIN64)
        #ifdef @LIB_NAME@_EXPORTS
            #define @LIB_NAME@_API __declspec(dllexport)
        #else
            #define @LIB_NAME@_API __declspec(dllimport)
        #endif
    #else
        #define @LIB_NAME@_API
    #endif
#endif
// clang-format on