# gstore 哈希索引落盘：插入与刷盘完整流程详解

本文按时间顺序从一条 INSERT 到达开始，逐函数、逐行追踪 gstore 哈希索引的写入路径，直到数据真正落到磁盘文件。所有引用都标明 `文件:行号`。

---

## 0. 总览：两文件、两层映射

gstore 的每个 hash index 在磁盘上是**两个文件**：

```
gstore_hash_index_<id>_i.dbf    ← 桶目录（array file），扁平 5 字节/槽
gstore_hash_index_<id>_ii.dbf   ← 键记录（key file），追加日志型 hrec 流
```

内存中两个对象分别管理：
- `t_hash_index_table`（`gdm_hash_index.h:49-62`）管理 array file
- `t_hash_index`（`gdm_hash_index.h:80-122`）管理 key file

每个 array 槽存 5 字节偏移，指向 key file 里的第一条 hrec；hrec 之间用 `prev_offset` 串成冲突链。这就是"桶目录 + 拉链日志"的形态。

---

## 1. 入口：从提交的事务到 hash index

事务提交后，segment 层会触发对每个索引的更新：

```
index_process_tuple(segment, tuple)              // src/segment/gdm_seg_manager.c:700
  └─ hash_index_process_tuple(...)               // :718
       └─ _hash_index_process_insert(...)        // gdm_hash_index.c:2133
            └─ _hash_index_process_insert_low(...)  // :2074
```

`_hash_index_process_insert_low`（`gdm_hash_index.c:2074-2102`）只做三件事：

```c
// 步骤 1: 计算 32 位主键 hash
fold = tuple->key->hash_code;

// 步骤 2: 由 32 位 hash 计算桶号
slot = _hash_index_table_calc_hash(hash_index->table, fold);

// 步骤 3: 加分段行锁，调下层插入
if (need_row_lock) htfile_row_lock(&row_lock, hash_index, slot);
hash_index_key_insert(hash_index, desc, tuple, 0, slot, loader_mode);
if (need_row_lock) htfile_row_unlock(&row_lock);
```

**关键细节：桶号怎么算**（`_hash_index_table_calc_hash`，`gdm_hash_index.c:986-997`）：
```c
t_uint64 _hash_index_table_calc_hash(t_hash_index_table* table, t_uint32 fold) {
    t_uint64 tt;
    tt = fold ^ HASH_INDEX_TABLE_RANDOM_MASK1;        // 1653893711
    tt = tt << 32;
    tt = tt | (fold ^ HASH_INDEX_TABLE_RANDOM_MASK2); // 1731231237
    return (tt % table->slot_count);                  // 取模得 slot
}
```
把 32 位 fold 用两个不同掩码异或拼成 64 位再 mod `slot_count`。这种"双掩码 + mod"是一种简单但分布尚可的整流（fold 已是 MurmurHash3 输出，分布本身就好）。

**分段行锁**（`ROW_LOCK_SIZE = 32`，`gdm_hash_index.h:46-47`）：
- `htfile_row_lock` 拿 `hash_index->row_lock_array[slot % 32]` 这把锁
- 同一桶 / 相邻桶的并发写会串行化，但 32 个分片让不同分片可以并行
- 这是为了避免每条记录都抢全局锁

---

## 2. 主插入函数 `hash_index_key_insert`

文件：`gdm_hash_index.c:1965-2071`。这是整个插入的核心，按 `table->mmap_flush` 标志分**两条完全不同的路径**。

### 2.1 内存态（`mmap_flush == FALSE`）—— 头插法

适用场景：批量导入、崩溃后的索引重建。代码 `gdm_hash_index.c:1985-2035`。

```c
if (!hash_index->table->mmap_flush) {
    if (loader_mode) {
        // 全量导入：连重复都不检查，直接读出当前槽里的旧 head
        table_data = hash_index_table_get_data(table, slot*5, ..., PAGE_PURPOSE_READ);
        table_off  = _hash_index_table_read_slot(table_data);
    } else {
        // 增量：检查冲突链上是否已存在相同主键
        key_off = _hash_index_key_search(..., cmp_id=FALSE, update_id=FALSE, ...);
        if (key_off != SINT40_UNDEFINED) return;   // 已存在 → 直接返回（不报错）
    }
    if (tuple) {
        hash_index_tuple_append(hash_index, desc, tuple,
                                insert_tail=FALSE, no_data=FALSE,
                                slot, prev_offset=table_off);
    }
}
```

要点：
- **头插法只需要知道当前槽里旧的 head 偏移**（`table_off`），不需要遍历整条链
- 新记录的 `prev_offset` 直接指向 `table_off`，槽位被改写为新记录的偏移
- O(1) 插入，但**断链风险**——崩溃时若新记录没刷盘而槽位已刷，链就断了。可接受，因为内存态崩溃会触发全量重建

### 2.2 mmap 态（`mmap_flush == TRUE`）—— 尾插法

适用场景：稳态运行。代码 `gdm_hash_index.c:2037-2068`。

```c
// 映射版用尾插法
if (tuple) {
    key_off = _hash_index_key_search(...,
                                    cmp_id=TRUE,    // 严格比对 rowid
                                    update_id=TRUE, // 命中不同 rowid 则原地刷新
                                    tuple,
                                    &no_data, &table_off, &last_off);
    if (key_off != SINT40_UNDEFINED) return;       // 已存在 → no-op

    hash_index_tuple_append(hash_index, desc, tuple,
                            insert_tail=TRUE, no_data,
                            slot, prev_offset=last_off);
}
```

要点：
- **尾插法必须先查到链尾**（`last_off`）才能挂上去——比头插法多一次链遍历
- 但**没有断链风险**：旧记录的 `prev_offset` 从 `SINT40_UNDEFINED` 改成新偏移，即使新偏移那条记录没刷盘，下次重启时只是少一条，链前段仍然完整
- `cmp_id=TRUE` 表示严格主键 + rowid 双判定，避免不同 rowid 的同主键被误判为重复

### 2.3 设计权衡（原文注释，`gdm_hash_index.c:1981-1984 / 2037-2039`）

```
内存版用头插法
1.头插法只需要知道数组内的文件偏移
2.头插法有断链的风险，需要能保证最后一次性落盘
3.当前版本只要是内存版，则表示是在导入或重建索引，都不需要查询Key重复

映射版用尾插法
1.尾插法需要知道上一条记录的偏移，因此必须去查询冲突链
2.尾插法没有断链风险，发现指向的Key文件不存在则重新补即可
```

这是 gstore 设计的核心权衡：**导入速度优先 vs 稳态崩溃安全**。

---

## 3. 冲突链遍历 `_hash_index_key_search`

文件：`gdm_hash_index.c:1780-1890`。这条函数同时承担"查重"和"找链尾"两个职责。

### 步骤拆解

**步骤 ①：读出桶槽里的链头偏移**（`:1802-1809`）
```c
table_data = hash_index_table_get_data(table, slot*5, ..., PAGE_PURPOSE_READ);
(*table_off) = _hash_index_table_read_slot(table_data);  // edn_read_5bytes
key_off = (*table_off);
```
- 每个槽 5 字节（`BYTES5_SIZE`），用 `edn_read_5bytes` 读取（小端 40 位）
- 通过 buffer pool 拿到 page 再读，见 `hash_index_table_get_data`（`:2589-`）

**步骤 ②：循环走链**（`:1812-1888`）
```c
while (key_off != SINT40_UNDEFINED) {
    // (a) 偏移合法性：不能超过当前 head
    if (key_off + HREC_HDR_SIZE + HREC_TAIL_SIZE > hash_index->head)
        return SINT40_UNDEFINED;

    // (b) 取出 hrec
    hrec = hash_index_get_hrec(hash_index, key_off, &blk, &page, PAGE_PURPOSE_READ);

    // (c) 逐记录崩溃校验：长度/越界/0xFF 魔术字节/checksum
    if (hrec_get_len(hrec) < HREC_HDR_SIZE + HREC_TAIL_SIZE ||
        (key_off + hrec_get_len(hrec) > blk->id*HASH_INDEX_MAP_BUF_SIZE + HASH_INDEX_MAP_BUF_SIZE) ||
        hrec_get_delimiter(hrec) != HREC_MAGIC_NUM ||
        hrec_calc_check_sum(hrec) != hrec_get_check_sum(hrec))
        return SINT40_UNDEFINED;

    (*last_off) = key_off;          // 记下链尾（每次循环都更新，退出时即真链尾）
    (*no_data)  = FALSE;

    // (d) 主键字节比较
    ret = hrec_th_cmp(key, hrec, desc, key->n_keys);
    if (ret == NCMP_IS_EQU) {
        // (e) 命中：按 cmp_id 决定是否还要比 rowid
        array_id = rowid_get_array_id_int36(key->row_id);
        if (!cmp_id || hrec_get_arrayid(hrec) == array_id) {
            return key_off;          // 真正命中
        }
        // (f) update_id 分支：原地刷新 array_id（loader 重建专用）
        if (update_id) {
            ... 加写锁、调 _hash_index_hrec_write_array_id ...
            return key_off;
        }
        return SINT40_UNDEFINED;     // 主键同 rowid 不同，继续当未命中
    }

    // (g) 主键不同，沿 prev_offset 走下一条
    key_off = hrec_get_prev_offset(hrec);
}
return SINT40_UNDEFINED;
```

### 冲突处理小结

- **冲突的物理表现**：两个不同主键被 `_hash_index_table_calc_hash` 映射到同一个 `slot`
- **冲突的解决方式**：在 key file 里追加新的 hrec，通过 `prev_offset` 串到链上
- **遍历复杂度**：O(链长)，平均 ≈ 1~2；最坏由实际数据量超出 array 估算值的程度决定
- **数据完整性**：每条 hrec 自带 `delimiter(0xFF) + checksum`，遍历时逐条校验，遇到坏记录立即截断

---

## 4. 追加 hrec 到 key file：`hash_index_tuple_append` → `_hash_index_buf_append`

### 4.1 `hash_index_tuple_append`（`:2563-2587`）

```c
// 根据 insert_tail 决定新记录的 prev_offset 初值
if (insert_tail) {
    code = hrec_encode(desc, tuple, hrec, SINT40_UNDEFINED);  // 尾插：先填非法值
} else {
    code = hrec_encode(desc, tuple, hrec, prev_offset);       // 头插：直接填旧 head
}
return _hash_index_buf_append(hash_index, hrec, hrec_get_len(hrec),
                              tuple, insert_tail, no_data, slot, prev_offset);
```

`hrec_encode`（`src/fsm/gdm_hrec.c`）把 tuple 编码成磁盘记录格式：
```
[len(2)][prev_offset(5)][arrayid(5)][null_flag(1)][hashcode(2)] [key data...] [checksum(1)][0xFF(1)]
  ← ──────────────── HREC_HDR (15B) ────────────────→            ← HREC_TAIL (2B) →
```
其中 `arrayid = rowid_get_array_id_int36(tuple->key->row_id)`、`hashcode = rowid_get_hash_code(...)`——和 RowID 同源（`gdm_hrec.c:82-83`）。

### 4.2 `_hash_index_buf_append`（`:2439-2560`）——真正的"落盘前缓存写入"

整个函数持有 `hash_index->flush_mutex`，步骤如下：

**① 计算写入位置，处理跨页**（`:2462-2474`）
```c
cur_head     = hash_index->head;                       // 当前末尾
cur_page_end = (cur_head / page_size) * page_size + page_size;

// 单条记录不超过一页；本页剩余不足就跳到下一页起写
if ((cur_page_end - cur_head) < rec_len) {
    skip_len   = (t_uint32)(cur_page_end - cur_head);  // 本页剩余被填 0
    cur_offset = cur_page_end + rec_len;
} else {
    cur_offset = cur_head + rec_len;
}
```
**关键约束**：单条 hrec 永远不跨页。这是为了崩溃恢复时按页扫描——遇到 0 字节即可停。

**② 计算块号、按需扩展内存块**（`:2476-2494`）
```c
block_index1 = cur_head / HASH_INDEX_MAP_BUF_SIZE;     // 1MB / 块
block_index2 = cur_offset / HASH_INDEX_MAP_BUF_SIZE;
_hash_index_blk_arr_check(hash_index, block_index2);   // 块数组不够就扩

if (!(hash_index->block[block_index1])) {
    hash_index->block[block_index1] = init_gdata_blk(..., FALSE);  // 内存块
    hash_index->block_count++;
}
```
key file 按 **1 MB（`HASH_INDEX_MAP_BUF_SIZE`）**为单位切内存块；按需创建。

**③ 把 hrec 字节拷进内存块**（`:2496-2537`）
- 不跨块：直接 `cf_memcpy(mdata + block_offset1, rec, rec_len)`
- 跨块：先填 0 跳过页尾，新块不存在则 `init_gdata_blk` 创建，再拷贝

**④ 挂接冲突链**（`:2541-2548`）——根据插法分流
```c
if (insert_tail) {
    _hash_index_key_write_tail(hash_index, no_data, slot, prev_offset, cur_offset - rec_len);
} else {
    _hash_index_key_write_head(hash_index, slot, cur_offset - rec_len);
}
```

**头插 `_hash_index_key_write_head`（`:1696-1707`）**：
```c
table_data = hash_index_table_get_data(table, slot*5, ..., PAGE_PURPOSE_WRITE);
_hash_index_table_write_slot(table_blk, table_data, key_off, page);
// 即把新记录偏移直接写入桶槽，覆盖旧 head
```
新 hrec 的 `prev_offset` 在步骤 4.1 已经填好（指向旧 head），所以链路完整。

**尾插 `_hash_index_key_write_tail`（`:1668-1693`）**：
```c
if (no_data) {
    // 链是空的：直接把新偏移写入桶槽
    _hash_index_table_write_slot(table_blk, table_data, key_off, page);
} else {
    // 链非空：把链尾 hrec 的 prev_offset 从 UNDEFINED 改成新偏移
    hrec = hash_index_get_hrec(hash_index, prev_off, &blk, &page, PAGE_PURPOSE_WRITE);
    _hash_index_hrec_write_prev_offset(blk, hrec, key_off, page);
}
```
注意 `_hash_index_hrec_write_prev_offset`（`:1640-1651`）改完 prev_offset 会**重算 checksum**，保证记录自校验仍然通过。

**⑤ 更新 meta 与 head**（`:2551-2552`）
```c
_hash_index_update_meta_by_tuple(hash_index, tuple);   // 更新 lsn/xid/last_vid 等
hash_index->head = cur_offset;                         // 推进 head
```

`_hash_index_update_meta_by_tuple`（`:1710-1740`）只对 `cur_meta` 取 MAX：
```c
meta->xid = MAX(meta->xid, tuple->xid);
meta->lsn = MAX(meta->lsn, tuple->lsn);
meta->last_vid = (t_uint32)MAX(meta->last_vid, array_id);  // 顶点
```
这是高水位线，不分配 ID（ID 由 `ver_sys->meta.last_vertex_id` 原子自增分配）。

**⑥ 增加脏数据计数**（`:2557`）
```c
hash_index_cache_size_add(manager, skip_len + rec_len);
```
当累计脏数据 ≥ `HASH_INDEX_MANAGER_FLUSH_SIZE`（16 MB）时，会唤醒后台 flush 线程。

### 4.3 至此"插入"完成——但数据还在内存里

整个 `_hash_index_buf_append` 只写了**内存块**（`block[i]` 的 `data`）。真正的磁盘 I/O 由后台 flush 线程异步完成。

---

## 5. 落盘：后台 flush 线程

### 5.1 触发条件（`_hash_index_flush`，`:3986-4005`）

```c
if (!(force_flush || hash_index_cache_size_full(manager) || ckpt_event || del_event))
    return;
```

四种触发：
1. **缓存到 16 MB**（`hash_index_cache_size_full`）
2. **检查点事件**（`ckpt_event`，由全局 checkpoint 触发）
3. **删除事件**（`del_event`，drop index 等）
4. **强制 flush**（`force_flush`，停机、loader 收尾等）

后台线程（`hash_index_flush_thread`，`:4178`）每 10ms 醒来检查一次，外加每 60s 一次定时强制 flush。

### 5.2 双 meta 冻结（`_hash_index_frozen_meta`，`:1753-1765`）

flush 之前最关键的一步——**指针交换实现无阻塞快照**：

```c
tmp_meta                = hash_index->cur_meta;
hash_index->cur_meta    = hash_index->frozen_meta;     // 旧的 frozen 拿来当新 cur
hash_index->frozen_meta = tmp_meta;                    // 旧 cur 升级成 frozen
cf_memset(hash_index->cur_meta, 0, sizeof(...));       // 新 cur 清零
hash_index->frozen_meta->head = hash_index->head;      // 冻结当前的 head/tail
hash_index->frozen_meta->tail = hash_index->tail;
```

效果：
- **新写入**继续往 `cur_meta` 累计，往 `head` 之后追加，**完全不受 flush 影响**
- **flush 线程**只看 `frozen_meta` 里冻结的快照，可以慢慢做 I/O

这就是 Shadow Paging 思想：通过原子指针交换，把"活跃写入区"和"刷盘快照区"解耦。

### 5.3 WAL 一致性检查（`:4031-4049`）

```c
if (hash_index->frozen_meta->lsn > segment->vlog_buf->tail && !segment_is_loader_mode(segment))
    flush_stop = TRUE;
```
**绝不允许 hash index 落盘的 LSN 超前于 vlog（WAL）**——否则崩溃时 WAL 重放无法恢复 hash index。如果超前了就停 flush，等 WAL 先落盘。

### 5.4 真正的 I/O：`try_write_hash_index_low`（`:3772-3891`）

整个函数持有 `flush_mutex`，按 `max_map_count` 把内存块分两类处理：

**① 尚未转 mmap 的纯内存块：`block[max_map_count .. block_count)`**

```c
for (i = hash_index->max_map_count; i < hash_index->block_count; i++) {
    if (i < head_blk_index) {
        // 整块刷
        os_interlock_set(&(blk->update_flag), 1);
        code = mem_write_gdata_blk(blk, 0, HASH_INDEX_MAP_BUF_SIZE);   // pwrite 整 1MB

        // 刷完后把内存块转成 mmap-dummy（不再占内存）
        mem_blk_to_mmap_blk_dummy(&(hash_index->block[i]), FALSE, NULL, FALSE);
        hash_index->max_map_count++;

        hash_index->tail = (i + 1) * HASH_INDEX_MAP_BUF_SIZE;
    } else {
        // 最后一块：部分刷，必须按 4KB 对齐
        write_len  = head % HASH_INDEX_MAP_BUF_SIZE;
        write_down_4k = (write_len >> 12) << 12;     // 4KB 对齐部分
        mem_write_buffer(blk, mdata, 0, write_down_4k);
        // 剩余不足 4K 的部分补 0 拼成 4K
        cf_memcpy(flush_buff, mdata + write_down_4k, write_len - write_down_4k);
        cf_memset(flush_buff + ..., 0, 4096 - ...);
        mem_write_buffer(blk, flush_buff, write_offset, 4096);

        hash_index->tail = head;                     // tail 追上 head
    }
}
```

要点：
- 文件用 `IO_ASYNC`（即 `O_DIRECT`）打开，所以**每次写必须 4KB 对齐**
- "整块刷"用 `mem_write_gdata_blk`，对应一次 `pwrite` 1MB
- "最后一块部分刷"按 4KB 向下对齐 + 末尾补零的 4KB 拼凑
- 刷完立刻 `mem_blk_to_mmap_blk_dummy`：把内存块释放、转成一个轻量的"占位"块，将来访问时按需 mmap
- `max_map_count` 是单调递增的"已落盘块数"

**② 已经是 mmap 的块：`block[0 .. max_map_count)`**

`_try_write_hash_index_key`（`:3874`）会调 buffer pool 的 `flush_page` 把脏 mmap 页写回：

```c
// _try_write_hash_index_table（:3877）类似
buffer_pool_base->flush_page(buffer_pool_base, &file_ident, offset, len);
```

这部分走标准的 buffer pool 刷脏路径。

### 5.5 刷文件头 `_meta_flush`（`:454`）

key file 头部 4KB（`HASH_INDEX_META_FLUSH`）保存：
```
[lsn(8)][xid(8)][last_vid(4)][last_eid(5)][last_rid(4)][offset(5)][mem_flag(1)]
```
其中 `offset = hash_index->tail`（已刷盘位置），`mem_flag = mmap_flush`（当前是 mmap 态还是内存态）。

写完后 `os_file_flush`（fsync）保证数据真的落到磁盘。

### 5.6 与全局检查点联动（`:4139`）

```c
ver_hash_add(segment->ver_sys, &ver_diff, ver_diff_num);
```
通知版本系统"hash index 已经刷到某个 LSN"，让 checkpoint 可以推进。

---

## 6. 完整时序图

```
[事务提交]
    │
    ▼
index_process_tuple(segment, tuple)
    │
    ▼
_hash_index_process_insert_low(hash_index, desc, tuple)
    │
    ├─ fold  = tuple->key->hash_code                  // 32 位主键 hash
    ├─ slot  = _hash_index_table_calc_hash(table, fold) // mod slot_count
    ├─ htfile_row_lock(slot % 32)                     // 加分段行锁
    │
    └─ hash_index_key_insert(hash_index, desc, tuple, slot)
          │
          ├─ [内存态 / 头插法]
          │     ├─ (loader_mode) 直接读出 table_off（旧 head）
          │     └─ (else) _hash_index_key_search(cmp_id=FALSE) 查重
          │
          ├─ [mmap 态 / 尾插法]
          │     └─ _hash_index_key_search(cmp_id=TRUE, update_id=TRUE)
          │           ├─ 读桶槽 5 字节得链头 table_off
          │           ├─ while (key_off != UNDEFINED):
          │           │     ├─ 校验 len / delimiter(0xFF) / checksum
          │           │     ├─ hrec_th_cmp 比主键
          │           │     ├─ 命中且 rowid 同 → 返回（重复，no-op）
          │           │     ├─ 命中且 rowid 异 + update_id → 原地刷 array_id
          │           │     └─ key_off = hrec_get_prev_offset  // 走链
          │           └─ last_off = 链尾偏移
          │
          └─ hash_index_tuple_append(...)
                │
                ├─ hrec_encode(desc, tuple, hrec, prev_offset)
                │     // 编码 15B 头 + key 数据 + 2B 尾（checksum + 0xFF）
                │
                └─ _hash_index_buf_append(hash_index, hrec, rec_len, ...)
                      │  [持有 flush_mutex]
                      │
                      ├─ 计算写入位置 cur_head，处理跨页
                      ├─ 按需创建 1MB 内存块
                      ├─ cf_memcpy 把 hrec 拷进 block[i]
                      │
                      ├─ [头插] _hash_index_key_write_head(slot, new_off)
                      │     └─ 桶槽 5 字节直接覆盖成 new_off
                      │        （新 hrec 的 prev_offset 已在 encode 时填好）
                      │
                      ├─ [尾插] _hash_index_key_write_tail(slot, last_off, new_off)
                      │     ├─ no_data=TRUE  → 桶槽写入 new_off
                      │     └─ no_data=FALSE → 改链尾 hrec 的 prev_offset=new_off
                      │                         并重算 checksum
                      │
                      ├─ _hash_index_update_meta_by_tuple
                      │     // cur_meta: xid/lsn/last_vid 取 MAX
                      │
                      ├─ hash_index->head = cur_offset       // 推进 head
                      └─ hash_index_cache_size_add(rec_len)  // 累计脏数据

[后台 flush 线程 每 10ms 醒来]
    │
    ▼
_hash_index_flush(manager, ckpt_event, del_event, force_flush)
    │  触发条件：脏数据 ≥16MB / 检查点 / 删除 / 强制
    │
    ├─ rlog_flush_for_db(...)                    // 先推 rlog/vlog
    │
    ├─ 对每个 hash_index 加全部 32 把行锁
    │     htfile_row_lock_all(hash_index)
    │
    ├─ _hash_index_frozen_meta(hash_index)       // 双 meta 指针交换
    │     cur_meta ↔ frozen_meta
    │     cur_meta 清零，新写入继续无阻塞
    │
    ├─ 检查 frozen_meta->lsn ≤ vlog->tail         // 不能超前 WAL
    │     否则 flush_stop=TRUE
    │
    ├─ 释放行锁，让写入继续
    │
    └─ try_write_hash_index_low(hash_index, head, tail)
          │  [持有 flush_mutex]
          │
          ├─ for i in [max_map_count .. block_count):
          │     ├─ 整块：pwrite 1MB → mem_write_gdata_blk
          │     │         mem_blk_to_mmap_blk_dummy  // 内存块转 mmap 占位
          │     │         max_map_count++
          │     └─ 末块：按 4KB 对齐 pwrite，末尾补零
          │
          ├─ _try_write_hash_index_key:    mmap 块走 buffer pool flush_page
          ├─ _try_write_hash_index_table:  array 文件 mmap 块同样刷
          │
          ├─ frozen_meta->offset = tail
          ├─ _meta_flush:  写 4KB 文件头 + fsync
          │     [lsn][xid][last_vid][last_eid][last_rid][offset][mem_flag]
          │
          └─ ver_hash_add: 通知 checkpoint 推进
```

---

## 7. 三个关键问题详解

### 7.1 "插入"和"落盘"是分开的吗？

**是的，完全解耦。**
- **插入路径**（`_hash_index_buf_append`）只往**内存块**拷字节，加 `flush_mutex`，**不做磁盘 I/O**
- **落盘路径**（后台线程 + `try_write_hash_index_low`）异步做真正的 `pwrite` + `fsync`

唯一的同步点是 `flush_mutex`——它保护 `head`/`tail`/`block[]` 等结构。但 flush 期间通过双 meta 冻结，写入路径几乎不会被阻塞。

### 7.2 同一条 hrec 在"插入后、落盘前"崩溃了怎么办？

这种情况下，内存块里的 hrec 丢失，但 **key file 末尾还是上次刷盘的状态**。重启时：
1. 读文件头 `mem_flag`
2. 如果是 mmap 态（正常停机）→ 走 `_hash_index_load(rebuild=FALSE)`，按 `offset` 增量重放未刷的 vlog 记录
3. 如果是内存态（崩溃在导入期）→ 删 array 文件，从 key file 头全量重放，重建桶目录

详见 `_hash_index_load_page`（`:1000-1051`）的逐记录校验逻辑。

### 7.3 头插法的"断链风险"具体指什么？

头插法在 `_hash_index_key_write_head` 里**先写新 hrec（在内存块里），后改桶槽指向新偏移**。如果在这两步之间崩溃，或者只刷了新 hrec 但桶槽还没刷，则：
- 桶槽仍指向旧 head → 旧链完整，新记录丢失（OK）
- 桶槽指向新偏移但新 hrec 没刷 → 链断了（坏）

但 gstore 在**内存态**用头插法，内存态崩溃时整个 array 文件本来就不可信（`mem_flag=FALSE`），会全量重建——所以**断链的代价就是重建**，这正是设计接受的。

mmap 态用尾插法就完全不同：改的是**旧记录**的 `prev_offset`，旧记录早已落盘，即使新 hrec 没刷，链前段仍然完整，最多丢最后一条。

---

## 8. 关键数据结构与常量速查

| 名称 | 值 | 含义 | 位置 |
|---|---|---|---|
| `BYTES5_SIZE` | 5 | 桶槽宽 / hrec 偏移字段宽 | `gdm_hrec.h:15` |
| `HREC_HDR_SIZE` | 15 | hrec 头长度 | `gdm_hrec.h:19` |
| `HREC_TAIL_SIZE` | 2 | hrec 尾（checksum + 0xFF） | `gdm_hrec.h:24` |
| `HREC_MAGIC_NUM` | 0xFF | hrec 结束魔术字节 | `gdm_hrec.h:10` |
| `HASH_INDEX_MAP_BUF_SIZE` | 1 MB | key file 内存块大小 | `gdm_hash_index.h:34` |
| `HASH_INDEX_TABLE_MAP_SIZE` | 5 MB | array file mmap 块大小 | `gdm_hash_index.h:29` |
| `HASH_INDEX_MANAGER_FLUSH_SIZE` | 16 MB | 触发 flush 的脏数据阈值 | `gdm_hash_index.h:22` |
| `ROW_LOCK_SIZE` | 32 | 分段行锁数 | `gdm_hash_index.h:46` |
| `HASH_INDEX_META_SIZE` | 64 KB | key file 头部预留 | `gdm_hash_index.h:43` |
| `HASH_INDEX_META_FLUSH` | 4 KB | key file 头部实际刷盘 | `gdm_hash_index.h:44` |

---

## 9. 函数索引

| 函数 | 文件:行 | 作用 |
|---|---|---|
| `_hash_index_process_insert_low` | `gdm_hash_index.c:2074` | 入口：算 slot + 加锁 |
| `_hash_index_table_calc_hash` | `gdm_hash_index.c:986` | 桶号计算 |
| `hash_index_key_insert` | `gdm_hash_index.c:1965` | 分头/尾插法主控 |
| `_hash_index_key_search` | `gdm_hash_index.c:1780` | 冲突链遍历 / 查重 / 找链尾 |
| `hash_index_tuple_append` | `gdm_hash_index.c:2563` | hrec 编码 + 调 buf_append |
| `hrec_encode` | `gdm_hrec.c` | tuple → 字节流 |
| `_hash_index_buf_append` | `gdm_hash_index.c:2439` | 写入内存块 + 挂链 + 推进 head |
| `_hash_index_key_write_head` | `gdm_hash_index.c:1696` | 头插：桶槽写新偏移 |
| `_hash_index_key_write_tail` | `gdm_hash_index.c:1668` | 尾插：链尾 prev_offset 改写 |
| `_hash_index_table_write_slot` | `gdm_hash_index.c:1621` | 桶槽 5 字节原子写 |
| `_hash_index_hrec_write_prev_offset` | `gdm_hash_index.c:1640` | hrec prev_offset 改写 + 重算 checksum |
| `_hash_index_update_meta_by_tuple` | `gdm_hash_index.c:1710` | cur_meta 取 MAX |
| `hash_index_flush_thread` | `gdm_hash_index.c:4178` | 后台刷盘线程 |
| `_hash_index_flush` | `gdm_hash_index.c:3986` | flush 主流程 |
| `_hash_index_frozen_meta` | `gdm_hash_index.c:1753` | 双 meta 指针交换 |
| `try_write_hash_index_low` | `gdm_hash_index.c:3772` | 真正的 pwrite I/O |
| `mem_write_gdata_blk` | gdata_blk 工具 | 整块 pwrite 1MB |
| `mem_blk_to_mmap_blk_dummy` | gdata_blk 工具 | 内存块转 mmap 占位 |
| `_meta_flush` | `gdm_hash_index.c:454` | 写 4KB 文件头 + fsync |

---

## 10. 一图总结

```
┌─────────────────────────────────────────────────────────────────┐
│                     内存态                                       │
│  ┌──────────────────┐    ┌──────────────────────────────────┐  │
│  │ cur_meta         │    │ block[]  (1MB 内存块数组)         │  │
│  │  - head          │ ←→ │  ┌──────┐ ┌──────┐ ┌──────┐      │  │
│  │  - tail          │    │  │ hrec │ │ hrec │ │ .... │      │  │
│  │  - lsn/xid/...   │    │  └──────┘ └──────┘ └──────┘      │  │
│  └──────────────────┘    │       ↑ head（追加点）           │  │
│         ↑↑↑               └──────────────────────────────────┘  │
│     新写入                  ↑ flush_mutex 保护                  │
└─────────────────────────────────────────────────────────────────┘
                            │ 后台线程：双 meta 冻结 → 交换指针
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     冻结快照                                     │
│  ┌──────────────────┐    ┌──────────────────────────────────┐  │
│  │ frozen_meta      │    │ block[max_map_count..]           │  │
│  │  - head/tail     │    │  整块 pwrite 1MB → 转成 mmap 占位 │  │
│  │  - lsn/xid/...   │    │ block[0..max_map_count]          │  │
│  └──────────────────┘    │  buffer_pool flush_page 刷脏      │  │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│            磁盘：两个 .dbf 文件                                   │
│                                                                  │
│  gstore_hash_index_<id>_i.dbf  (桶目录)                          │
│   [slot0(5B)][slot1(5B)]...[slotN(5B)]                          │
│       ↓                                                          │
│       └──→ key file 偏移                                         │
│                                                                  │
│  gstore_hash_index_<id>_ii.dbf (键记录日志)                      │
│   [4KB 文件头][1MB 块0][1MB 块1]...                              │
│                 ↓                                                │
│                 hrec: [len][prev_off][arrayid][null][hc][KEY][cs][0xFF] │
│                       ↑                                          │
│                       └── prev_off 串成冲突链                     │
└─────────────────────────────────────────────────────────────────┘
```
